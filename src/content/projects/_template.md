---
# COPY THIS FILE TO ADD A PROJECT
# -------------------------------
# Save it as `my-project.md` in this same folder. The leading underscore is what
# keeps this template itself off the site, so do not start your filename with one.
# Every field is validated when you build, so `npm run check` will tell you
# precisely what is wrong rather than shipping a broken page.

# Product name, as a person would say it. Shown as the project heading.
title: Project Name

# One line under the title. Under 90 characters. Say what it is, not how good it is.
tagline: The one line that says what this is

# Plain-text description, 40 to 320 characters. Used for search engines and social
# cards, which cannot read Markdown, so keep it plain and self-contained.
summary: A plain-text sentence describing what this project is and the problem it solves, written so it makes sense on its own with no surrounding context.

# Broad grouping. Shown as metadata. e.g. Reading, Media, Community, Health.
category: Category

# Where a person can actually use it. Leave as [] to hide the row.
platforms: [iOS, Android]

# One of exactly: Shipped, Beta, In development, Prototype
status: In development

# "2026", "2025 - 2026", or "2025 - present"
year: '2026'

# Up to four short capability phrases, six words or so each. These are the
# leaders in the entry. Written for a person, not a spec sheet.
highlights:
  - First thing it does well
  - Second thing it does well
  - Third thing it does well

# Optional. Outbound links, shown in listed order. Delete the block if there are none.
# links:
#   - label: App Store
#     href: https://apps.apple.com/app/id000000000
#   - label: Source
#     href: https://github.com/you/project

# Optional per-project accent. Falls back to the site accent when left out.
# accent: '#E2431F'

# Lower numbers appear first on the page.
order: 100

# Set to true to keep this file in the repo but off the live site.
draft: true
---

The paragraph a visitor actually reads. Aim for 45 to 75 words. Write about what
the person using it gets, not how it was built. Plain sentences beat adjectives.
